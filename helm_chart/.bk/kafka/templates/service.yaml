apiVersion: v1
kind: Service
metadata:
  name: {{ include ".bk.fullname" . }}
  labels:
    {{- include ".bk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include ".bk.selectorLabels" . | nindent 4 }}
